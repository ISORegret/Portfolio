# Copy this file to `.env.local` and fill in your live site URL (no trailing slash).
# Used for canonical URLs, Open Graph, and metadata in dev when you want them to match production.
#
#   cp .env.example .env.local   (Mac/Linux)
#   copy .env.example .env.local (Windows CMD)
#
NEXT_PUBLIC_SITE_URL=https://www.isoregret.com
